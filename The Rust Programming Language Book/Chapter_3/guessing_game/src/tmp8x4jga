use std::io;

fn main() {
    pritln!("Guess the number!");
    pritln!("Please input your guess.");

    let mut guess = String::new(); // cr
    io::stdin().read_line(&mut guess)
        .ok()
        .expect("Failed to read line");

    println!("You guessed {}", guess);
}
